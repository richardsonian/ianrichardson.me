source 'https://rubygems.org'

#gem 'jekyll'
gem "github-pages", group: :jekyll_plugins

#plugins
#jekyll-admin not supported with github pages
#gem 'jekyll-admin', group: :jekyll_plugins
group :jekyll_plugins do
    # plugins here?
end